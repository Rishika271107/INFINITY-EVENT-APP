{
  "name": "infinity-event-app-root",
  "version": "1.0.0",
  "description": "Root proxy package.json for Render Monorepo deployment",
  "private": true,
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd backend && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": "18.x"
  }
}
