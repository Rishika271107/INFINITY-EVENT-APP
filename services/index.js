// Services layer — ready for future business logic extraction
// Examples: EmailService, PaymentService, NotificationService, AIService
//
// Usage pattern:
//   const emailService = require("./emailService");
//   await emailService.sendBookingConfirmation(user, booking);
//
// This layer sits between controllers and models to keep controllers thin.
// Add service files here as the application grows.

module.exports = {};
